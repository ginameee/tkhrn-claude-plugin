{
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/save-handoff.sh\"",
            "timeout": 30,
            "statusMessage": "Saving handoff document..."
          }
        ]
      }
    ]
  }
}
